{
    "name": "eb-tokens-api",
    "version": "0.0.1",
    "description": "A REST API for generating and validating Echobind Tokens.",
    "scripts": {
        "server:start": "NODE_ENV=production node server/app.js",
        "server:start:test": "NODE_ENV=testing nodemon -L --watch ./server ./server/app.js",
        "server:start:dev": "NODE_ENV=development nodemon -L --watch ./server ./server/app.js",
        "test": "jest ./server",
        "lint": "esw --watch --color ./server"
    },
    "author": "Cully Larson <cully.larson@gmail.com>",
    "_moduleAliases": {
        "@@app": "./server"
    },
    "jest": {
        "moduleNameMapper": {
            "^@@app(.*)$": "<rootDir>/server$1"
        }
    },
    "devDependencies": {
        "eslint": "^7.25.0",
        "eslint-plugin-jest": "^24.3.6",
        "eslint-watch": "^7.0.0",
        "jest": "^26.6.3",
        "nodemon": "^2.0.7",
        "standard": "^16.0.3"
    },
    "dependencies": {
        "@cullylarson/p": "^0.1.0",
        "@cullylarson/validate": "^1.0.15",
        "body-parser": "^1.19.0",
        "cors": "^2.8.5",
        "express": "^4.17.1",
        "module-alias": "^2.2.2"
    }
}
